# 项目规范与记录

当用户说 **"记一下"** 时，表示用户要求将当前讨论的结论、规范、要点或经验记录下来。
请使用 `edit` 或 `write` 工具将内容追加/写入此文件。

---

## 记录内容格式

每条记录应包含：
- 记录的日期（如果可推断）
- 记录的主题/标题
- 具体内容

格式示例：
```
### 2026-05-28 - 项目命名规范
- 组件文件使用 PascalCase
- 工具函数使用 camelCase
- 常量使用 UPPER_SNAKE_CASE
```

---

